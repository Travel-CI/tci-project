eureka:
  client:
    enabled: false

spring:
  profiles:
    active: test

info:
  repositories:
    #location: /tmp/travel-ci/
    location: C:/Temp/
  services:
    commands: http://localhost:8082
    logger: http://localhost:8083